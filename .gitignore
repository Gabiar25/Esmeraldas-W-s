# Fotos originales sin comprimir (varios cientos de MB) - el sitio usa las
# copias optimizadas en backend/public/assets/images, no estas.
Joyeria-Willi/

# Dependencias de Node (se reinstalan con "npm install")
backend/node_modules/

# Secretos - nunca subir llaves reales a un repositorio
backend/.env

# Archivos temporales del sistema
.DS_Store
Thumbs.db

# Copia local opcional del feed de Meta (el feed real se sirve al vuelo
# desde server.js, ver scripts/generate_meta_feed.js)
backend/meta-catalog-feed.local.csv
